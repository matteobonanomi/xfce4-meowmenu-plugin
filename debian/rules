#!/usr/bin/make -f
# Standard debhelper Meson invocation with optional desktop integrations
# disabled for the portable distribution package.

%:
	dh $@ --buildsystem=meson

override_dh_auto_configure:
	dh_auto_configure -- \
		-Daccountsservice=disabled \
		-Dgtk-layer-shell=disabled
