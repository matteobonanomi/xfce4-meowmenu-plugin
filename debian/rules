#!/usr/bin/make -f
# Standard debhelper meson invocation. The package is built with project
# defaults (no sanitizer / no-optional-deps overrides — those are CI-only
# configurations).

%:
	dh $@ --buildsystem=meson
